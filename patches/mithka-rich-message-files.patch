diff --git a/td/generate/scheme/td_api.tl b/td/generate/scheme/td_api.tl
index 4dac6c8b1..b0d877945 100644
--- a/td/generate/scheme/td_api.tl
+++ b/td/generate/scheme/td_api.tl
@@ -133,6 +132,0 @@ richMessageSourceHtml text:string = RichMessageSource;
-//@description A rich message to send
-//@source Source of the rich message
-//@is_rtl Pass true if the message must be shown from right to left
-//@detect_automatic_blocks Pass true to enable detection of URLs, email addresses and other automatic blocks
-inputRichMessage source:RichMessageSource is_rtl:Bool detect_automatic_blocks:Bool = InputRichMessage;
-
@@ -5687,0 +5682,24 @@ inputVideo video:InputFile thumbnail:inputThumbnail cover:InputFile start_timest
+//@class InputRichMessageFile @description Describes a local or remote file referenced from rich message source
+
+//@description A photo referenced from rich message source @id Identifier used as the media source in the rich message @photo Photo to be sent
+inputRichMessageFilePhoto id:string photo:inputPhoto = InputRichMessageFile;
+
+//@description An animation referenced from rich message source @id Identifier used as the media source in the rich message @animation Animation to be sent
+inputRichMessageFileAnimation id:string animation:inputAnimation = InputRichMessageFile;
+
+//@description An audio file referenced from rich message source @id Identifier used as the media source in the rich message @audio Audio file to be sent
+inputRichMessageFileAudio id:string audio:inputAudio = InputRichMessageFile;
+
+//@description A document referenced from rich message source @id Identifier used as the media source in the rich message @document Document to be sent
+inputRichMessageFileDocument id:string document:inputDocument = InputRichMessageFile;
+
+//@description A video referenced from rich message source @id Identifier used as the media source in the rich message @video Video to be sent
+inputRichMessageFileVideo id:string video:inputVideo = InputRichMessageFile;
+
+//@description A rich message to send
+//@source Source of the rich message
+//@is_rtl Pass true if the message must be shown from right to left
+//@detect_automatic_blocks Pass true to enable detection of URLs, email addresses and other automatic blocks
+//@files Files referenced from the rich message source
+inputRichMessage source:RichMessageSource is_rtl:Bool detect_automatic_blocks:Bool files:vector<InputRichMessageFile> = InputRichMessage;
+
diff --git a/td/telegram/MessageContent.cpp b/td/telegram/MessageContent.cpp
index 953e5531a..5e3472248 100644
--- a/td/telegram/MessageContent.cpp
+++ b/td/telegram/MessageContent.cpp
@@ -5680,0 +5681,8 @@ static telegram_api::object_ptr<telegram_api::InputMedia> get_message_content_in
+    case MessageContentType::RichText: {
+      const auto *m = static_cast<const MessageRichText *>(content);
+      if (media_pos < 0 || media_pos >= m->text.get_input_file_count()) {
+        return nullptr;
+      }
+      return get_message_content_input_media_impl(m->text.get_input_file_content(media_pos), -1, td,
+                                                  std::move(input_file), std::move(input_thumbnail), ttl, emoji);
+    }
@@ -5712 +5719,0 @@ static telegram_api::object_ptr<telegram_api::InputMedia> get_message_content_in
-    case MessageContentType::RichText:
@@ -6357 +6364 @@ bool can_forward_message_content(const Td *td, const MessageContent *content, bo
-    return !is_copy || td->auth_manager_->is_bot();
+    return true;
@@ -10031 +10038 @@ unique_ptr<MessageContent> dup_message_content(Td *td, DialogId dialog_id, const
-      return make_unique<MessageRichText>(message_rich_text->text.clone());
+      return make_unique<MessageRichText>(message_rich_text->text.clone_with_input_files(td, dialog_id));
@@ -12067,0 +12075,10 @@ unique_ptr<MessageContent> get_uploaded_message_content(
+      case MessageContentType::RichText: {
+        auto rich_text = static_cast<const MessageRichText *>(old_content);
+        CHECK(media_pos < rich_text->text.get_input_file_count());
+        auto content = make_unique<MessageRichText>(rich_text->text.clone_with_input_files(td, owner_dialog_id));
+        auto file_content = rich_text->text.get_input_file_content(media_pos);
+        content->text.set_input_file_content(
+            media_pos, get_uploaded_message_content(td, file_content, -1, std::move(media_ptr), owner_dialog_id,
+                                                    message_date, source));
+        return std::move(content);
+      }
@@ -12281,0 +12299,4 @@ vector<FileId> get_message_content_any_file_ids(const Td *td, const MessageConte
+  if (content_type == MessageContentType::RichText) {
+    return transform(static_cast<const MessageRichText *>(content)->text.get_input_file_contents(),
+                     [](const MessageContent *file) { return get_message_content_any_file_id(file); });
+  }
@@ -12396,0 +12418,4 @@ vector<FileId> get_message_content_thumbnail_file_ids(const MessageContent *cont
+  if (content_type == MessageContentType::RichText) {
+    return transform(static_cast<const MessageRichText *>(content)->text.get_input_file_contents(),
+                     [td](const MessageContent *file) { return get_message_content_thumbnail_file_id(file, td); });
+  }
diff --git a/td/telegram/MessageContentType.cpp b/td/telegram/MessageContentType.cpp
index 5e3bf1561..84f942ba0 100644
--- a/td/telegram/MessageContentType.cpp
+++ b/td/telegram/MessageContentType.cpp
@@ -361,0 +362 @@ bool can_message_content_have_multiple_files(MessageContentType content_type) {
+    case MessageContentType::RichText:
diff --git a/td/telegram/MessagesManager.cpp b/td/telegram/MessagesManager.cpp
index 334350950..5e63d12ec 100644
--- a/td/telegram/MessagesManager.cpp
+++ b/td/telegram/MessagesManager.cpp
@@ -21709 +21709,2 @@ void MessagesManager::do_send_message(DialogId dialog_id, const Message *m, int3
-  if (content_type == MessageContentType::Text || content_type == MessageContentType::RichText) {
+  if (content_type == MessageContentType::Text ||
+      (content_type == MessageContentType::RichText && get_message_content_any_file_ids(td_, content).empty())) {
@@ -22375,0 +22377,15 @@ void MessagesManager::do_send_internal_media_group(DialogId dialog_id, MessageId
+  if (m->content->get_type() == MessageContentType::RichText) {
+    auto input_rich_message = get_message_content_input_rich_message(td_, m->content.get());
+    if (input_rich_message == nullptr) {
+      pending_internal_media_sends_.erase(it);
+      return on_send_message_fail(random_id, Status::Error(400, "Failed to prepare rich message media"));
+    }
+    pending_internal_media_sends_.erase(it);
+    td_->create_handler<SendMessageQuery>()->send(
+        get_message_flags(m), dialog_id, get_send_message_as_input_peer(m), *get_message_input_reply_to(m),
+        get_send_message_topic(dialog_id, m), get_message_schedule_date(m), get_message_schedule_repeat_period(m),
+        m->effect_id, m->paid_message_star_count, m->suggested_post.get(), m->reply_markup, FormattedText(),
+        std::move(input_rich_message), m->is_copy, random_id, &m->send_query_ref);
+    return;
+  }
+
diff --git a/td/telegram/RichMessage.cpp b/td/telegram/RichMessage.cpp
index 5af6ec210..f95894469 100644
--- a/td/telegram/RichMessage.cpp
+++ b/td/telegram/RichMessage.cpp
@@ -14,0 +15,2 @@
+#include "td/telegram/MessageContent.h"
+#include "td/telegram/OptionManager.h"
@@ -22,0 +25 @@
+#include "td/utils/FlatHashSet.h"
@@ -26,0 +30,12 @@ namespace td {
+RichMessage::InputFile::InputFile(string id, unique_ptr<MessageContent> content)
+    : id(std::move(id)), content(std::move(content)) {
+}
+
+RichMessage::InputFile::InputFile(InputFile &&) noexcept = default;
+
+RichMessage::InputFile &RichMessage::InputFile::operator=(InputFile &&) noexcept = default;
+
+RichMessage::InputFile::~InputFile() = default;
+
+RichMessage::~RichMessage() = default;
+
@@ -114,0 +130,60 @@ Result<RichMessage> RichMessage::get_rich_message(Td *td, DialogId dialog_id,
+  FlatHashSet<string> file_ids;
+  for (auto &file : message->files_) {
+    if (file == nullptr) {
+      return Status::Error(400, "Rich message file must be non-empty");
+    }
+    auto id = [&]() -> string {
+      switch (file->get_id()) {
+        case td_api::inputRichMessageFilePhoto::ID:
+          return static_cast<td_api::inputRichMessageFilePhoto *>(file.get())->id_;
+        case td_api::inputRichMessageFileAnimation::ID:
+          return static_cast<td_api::inputRichMessageFileAnimation *>(file.get())->id_;
+        case td_api::inputRichMessageFileAudio::ID:
+          return static_cast<td_api::inputRichMessageFileAudio *>(file.get())->id_;
+        case td_api::inputRichMessageFileDocument::ID:
+          return static_cast<td_api::inputRichMessageFileDocument *>(file.get())->id_;
+        case td_api::inputRichMessageFileVideo::ID:
+          return static_cast<td_api::inputRichMessageFileVideo *>(file.get())->id_;
+        default:
+          UNREACHABLE();
+          return string();
+      }
+    }();
+    if (id.empty() || !clean_input_string(id) || !file_ids.insert(id).second) {
+      return Status::Error(400, "Rich message file identifiers must be non-empty and unique");
+    }
+    auto input_message_content = [&]() -> td_api::object_ptr<td_api::InputMessageContent> {
+      switch (file->get_id()) {
+        case td_api::inputRichMessageFilePhoto::ID: {
+          auto value = td_api::move_object_as<td_api::inputRichMessageFilePhoto>(file);
+          return td_api::make_object<td_api::inputMessagePhoto>(std::move(value->photo_), nullptr, false, nullptr,
+                                                                false);
+        }
+        case td_api::inputRichMessageFileAnimation::ID: {
+          auto value = td_api::move_object_as<td_api::inputRichMessageFileAnimation>(file);
+          return td_api::make_object<td_api::inputMessageAnimation>(std::move(value->animation_), nullptr, false,
+                                                                    false);
+        }
+        case td_api::inputRichMessageFileAudio::ID: {
+          auto value = td_api::move_object_as<td_api::inputRichMessageFileAudio>(file);
+          return td_api::make_object<td_api::inputMessageAudio>(std::move(value->audio_), nullptr);
+        }
+        case td_api::inputRichMessageFileDocument::ID: {
+          auto value = td_api::move_object_as<td_api::inputRichMessageFileDocument>(file);
+          return td_api::make_object<td_api::inputMessageDocument>(std::move(value->document_), nullptr);
+        }
+        case td_api::inputRichMessageFileVideo::ID: {
+          auto value = td_api::move_object_as<td_api::inputRichMessageFileVideo>(file);
+          return td_api::make_object<td_api::inputMessageVideo>(std::move(value->video_), nullptr, false, nullptr,
+                                                                false);
+        }
+        default:
+          UNREACHABLE();
+          return nullptr;
+      }
+    }();
+    TRY_RESULT(input_content,
+               get_input_message_content(dialog_id, std::move(input_message_content), td,
+                                         td->option_manager_->get_option_boolean("is_premium")));
+    rich_message.input_files_.emplace_back(std::move(id), std::move(input_content.content));
+  }
@@ -133,0 +209,22 @@ void RichMessage::append_file_ids(const Td *td, vector<FileId> &file_ids) const
+  for (const auto &file : input_files_) {
+    append(file_ids, get_message_content_file_ids(file.content.get(), td));
+  }
+}
+
+vector<const MessageContent *> RichMessage::get_input_file_contents() const {
+  return transform(input_files_, [](const InputFile &file) { return file.content.get(); });
+}
+
+int32 RichMessage::get_input_file_count() const {
+  return narrow_cast<int32>(input_files_.size());
+}
+
+const MessageContent *RichMessage::get_input_file_content(int32 pos) const {
+  CHECK(pos >= 0 && static_cast<size_t>(pos) < input_files_.size());
+  return input_files_[pos].content.get();
+}
+
+void RichMessage::set_input_file_content(int32 pos, unique_ptr<MessageContent> content) {
+  CHECK(pos >= 0 && static_cast<size_t>(pos) < input_files_.size());
+  CHECK(content != nullptr);
+  input_files_[pos].content = std::move(content);
@@ -197,0 +295,27 @@ telegram_api::object_ptr<telegram_api::InputRichMessage> RichMessage::get_input_
+  vector<telegram_api::object_ptr<telegram_api::InputRichFile>> input_files;
+  input_files.reserve(input_files_.size());
+  for (const auto &file : input_files_) {
+    auto input_media = get_message_content_input_media(file.content.get(), const_cast<Td *>(td),
+                                                       MessageSelfDestructType(), string(), true, -1);
+    if (input_media == nullptr) {
+      LOG(ERROR) << "Failed to resolve rich message file " << file.id;
+      return nullptr;
+    }
+    switch (input_media->get_id()) {
+      case telegram_api::inputMediaPhoto::ID: {
+        auto media = telegram_api::move_object_as<telegram_api::inputMediaPhoto>(input_media);
+        input_files.push_back(
+            telegram_api::make_object<telegram_api::inputRichFilePhoto>(file.id, std::move(media->id_)));
+        break;
+      }
+      case telegram_api::inputMediaDocument::ID: {
+        auto media = telegram_api::move_object_as<telegram_api::inputMediaDocument>(input_media);
+        input_files.push_back(
+            telegram_api::make_object<telegram_api::inputRichFileDocument>(file.id, std::move(media->id_)));
+        break;
+      }
+      default:
+        LOG(ERROR) << "Receive unsupported rich message input media " << to_string(input_media);
+        return nullptr;
+    }
+  }
@@ -222,2 +346,3 @@ telegram_api::object_ptr<telegram_api::InputRichMessage> RichMessage::get_input_
-      return telegram_api::make_object<telegram_api::inputRichMessageMarkdown>(0, is_rtl_, noautolink_, source_,
-                                                                               Auto());
+      return telegram_api::make_object<telegram_api::inputRichMessageMarkdown>(
+          input_files.empty() ? 0 : telegram_api::inputRichMessageMarkdown::FILES_MASK, is_rtl_, noautolink_, source_,
+          std::move(input_files));
@@ -225 +350,3 @@ telegram_api::object_ptr<telegram_api::InputRichMessage> RichMessage::get_input_
-      return telegram_api::make_object<telegram_api::inputRichMessageHTML>(0, is_rtl_, noautolink_, source_, Auto());
+      return telegram_api::make_object<telegram_api::inputRichMessageHTML>(
+          input_files.empty() ? 0 : telegram_api::inputRichMessageHTML::FILES_MASK, is_rtl_, noautolink_, source_,
+          std::move(input_files));
@@ -247,0 +375,10 @@ RichMessage RichMessage::clone() const {
+RichMessage RichMessage::clone_with_input_files(Td *td, DialogId dialog_id) const {
+  auto result = clone();
+  for (const auto &file : input_files_) {
+    result.input_files_.emplace_back(
+        file.id, dup_message_content(td, dialog_id, file.content.get(), MessageContentDupType::Forward,
+                                     MessageCopyOptions()));
+  }
+  return result;
+}
+
diff --git a/td/telegram/RichMessage.h b/td/telegram/RichMessage.h
index 750d9960a..78ce53986 100644
--- a/td/telegram/RichMessage.h
+++ b/td/telegram/RichMessage.h
@@ -27,0 +28,2 @@ class Dependencies;
+class MessageContent;
+
@@ -30,0 +33,10 @@ class RichMessage {
+  struct InputFile {
+    string id;
+    unique_ptr<MessageContent> content;
+
+    InputFile(string id, unique_ptr<MessageContent> content);
+    InputFile(InputFile &&) noexcept;
+    InputFile &operator=(InputFile &&) noexcept;
+    ~InputFile();
+  };
+
@@ -39,0 +52 @@ class RichMessage {
+  vector<InputFile> input_files_;
@@ -49 +62 @@ class RichMessage {
-  ~RichMessage() = default;
+  ~RichMessage();
@@ -64,0 +78,8 @@ class RichMessage {
+  vector<const MessageContent *> get_input_file_contents() const;
+
+  int32 get_input_file_count() const;
+
+  const MessageContent *get_input_file_content(int32 pos) const;
+
+  void set_input_file_content(int32 pos, unique_ptr<MessageContent> content);
+
@@ -86,0 +108,2 @@ class RichMessage {
+  RichMessage clone_with_input_files(Td *td, DialogId dialog_id) const;
+
diff --git a/td/telegram/RichMessage.hpp b/td/telegram/RichMessage.hpp
index 161c63161..a3f77ff60 100644
--- a/td/telegram/RichMessage.hpp
+++ b/td/telegram/RichMessage.hpp
@@ -9,0 +10 @@
+#include "td/telegram/MessageContent.h"
@@ -18,0 +20 @@ void RichMessage::store(StorerT &storer) const {
+  bool has_input_files = !input_files_.empty();
@@ -24,0 +27 @@ void RichMessage::store(StorerT &storer) const {
+  STORE_FLAG(has_input_files);
@@ -32,0 +36,7 @@ void RichMessage::store(StorerT &storer) const {
+  if (has_input_files) {
+    td::store(narrow_cast<int32>(input_files_.size()), storer);
+    for (const auto &file : input_files_) {
+      td::store(file.id, storer);
+      store_message_content(file.content.get(), storer);
+    }
+  }
@@ -38,0 +49 @@ void RichMessage::parse(ParserT &parser) {
+  bool has_input_files;
@@ -44,0 +56 @@ void RichMessage::parse(ParserT &parser) {
+  PARSE_FLAG(has_input_files);
@@ -52,0 +65,15 @@ void RichMessage::parse(ParserT &parser) {
+  if (has_input_files) {
+    int32 input_file_count;
+    td::parse(input_file_count, parser);
+    if (input_file_count < 0 || input_file_count > 100) {
+      return parser.set_error("Invalid rich message input file count");
+    }
+    input_files_.reserve(input_file_count);
+    for (int32 i = 0; i < input_file_count; i++) {
+      string id;
+      unique_ptr<MessageContent> content;
+      td::parse(id, parser);
+      parse_message_content(content, parser);
+      input_files_.emplace_back(std::move(id), std::move(content));
+    }
+  }
